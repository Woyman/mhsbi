{
  "name": "mhsbi-portfolio-onepage",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "serve . -l 5173",
    "start": "serve . -l 5173",
    "build": "rm -rf public && mkdir -p public && cp index.html public/index.html && cp -R assets public/",
    "preview": "serve public -l 4173"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
